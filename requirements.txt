# Core FastAPI dependencies
fastapi==0.115.0
uvicorn==0.32.0

# For environment variables and templating
python-dotenv==1.0.1
Jinja2==3.1.3

# Geospatial / planning utilities (safe)
shapely==2.0.5
geojson==3.1.0
pandas==2.2.2

# Build tools (to avoid setuptools.build_meta error)
setuptools>=75.1.0
wheel>=0.44.0
pip>=25.3
